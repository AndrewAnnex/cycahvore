name: PR and Merge builds (on main branch)

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  build_x86:
    name: Test cycahvore
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        cibw_python: [ "cp310-*", "cp311-*", "cp312-*" ]
        cibw_manylinux: [ manylinux2014 ]
        cibw_arch: [ "auto64" ]
    steps:
      - name: Checkout cycahvore
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Windows disable protectNTFS
        if: runner.os == 'Windows'
        run:
          git config --global core.protectNTFS false
      - name: Checkout VICAR
        uses: actions/checkout@v4
        with:
          repository: NASA-AMMOS/VICAR
          path: "src/VICAR"
          fetch-depth: null
          filter: blob:none
      - name: ls
        run: |
          ls ./*
          ls ./*/*
      - name: Set up Python 🐍 ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          check-latest: true
      - name: Install pip
        run: |
          python -m pip install --upgrade pip
      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel
      - name: Build the wheel
        env:
          CIBW_BUILD: ${{ matrix.cibw_python }}
          CIBW_ARCHS_LINUX: ${{ matrix.cibw_arch }}
          CIBW_SKIP: "*-win32 *-manylinux_i686 *-musllinux_i686 pypy* pp* *-musllinux_aarch64 *-musllinux_x86_64"
          CIBW_MANYLINUX_X86_64_IMAGE: ${{ matrix.cibw_manylinux }}
        run: |
          env | grep CIBW
          env | grep ARCH
          python -m cibuildwheel --output-dir dist
      - uses: actions/upload-artifact@v3
        with:
          name: wheels
          path: ./dist/*.whl
